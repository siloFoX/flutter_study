import "package:flutter/material.dart";

import "package:calendar_scheduler/screen/home_screen.dart";

void main() {
    runApp(
        MaterialApp(
            home : HomeScreen(),
        ),
    );
}