android {
  compileSdkVersion 32
  
  defaultConfig {
    applicationId "com.example.blog_web_app"
    minSdkVersion 20
    targetSdkVersion flutter.targetSdkVersion
    versionCode flutterVersionCode.toInteget()
    versionName flutterVersionName
  }
}